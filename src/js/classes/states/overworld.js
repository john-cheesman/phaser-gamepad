import {Area} from '../area';

export class Overworld extends Area {
    constructor(tileMap) {
        super(tileMap);
    }

    preload() {
        super.preload();
    }

    create() {
        super.create();
    }

    update() {
        super.update();
    }
}
